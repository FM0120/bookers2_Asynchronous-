<% if user_signed_in? %>

<% if book.favorited_by?(current_user) %>  
   <%= link_to book_favorite_path(book.id), method: :delete, class: 'btn btn-default', remote: true do %>
       ♥<%= book.favorites.count %>
   <% end %>

<% else %>
      <%= link_to book_favorite_path(book.id), method: :post, class: 'btn btn-default', remote: true do %>
         ♡<%= book.favorites.count %>
      <% end %>
  <% end %>
<% end %>